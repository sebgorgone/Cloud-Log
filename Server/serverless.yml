service: cloud-log-api
provider:
  name: aws
  runtime: nodejs20.x
  region: us-east-2
  timeout: 30  
  environment:
    DB_HOST: '{{resolve:secretsmanager:CLDB:SecretString:DB_HOST}}'
    DB_USER: '{{resolve:secretsmanager:CLDB:SecretString:DB_USER}}'
    DB_PASSWORD: '{{resolve:secretsmanager:CLDB:SecretString:DB_PASSWORD}}'
    DB_NAME: '{{resolve:secretsmanager:CLDB:SecretString:DB_NAME}}'
    JWT_SECRET: '{{resolve:secretsmanager:CLDB:SecretString:JWT_SECRET}}'
    CORS_ORIGINS: '{{resolve:secretsmanager:CLDB:SecretString:CORS_ORIGINS}}'     
  vpc:
    securityGroupIds:
      - sg-06b4c181835350def
    subnetIds:
      - subnet-048b31cc2b20b55c7
  iamRoleStatements:
    - Effect: "Allow"
      Action:
        - "secretsmanager:GetSecretValue"
      Resource:
        - "arn:aws:secretsmanager:us-east-2:*:secret:CLDB*"

functions:
  api:
    handler: server.handler
    events:
      - http:
          path: /{proxy+}
          method: any
          cors:
            origin:
              - 'https://main.dmade3q40chsh.amplifyapp.com'
            allowCredentials: true
            headers:
              - Content-Type
              - Authorization
            methods:
              - OPTIONS
              - GET
              - POST
              - PUT
              - DELETE
              - PATCH

resources:
  Resources:
    SecretsManagerVpcEndpoint:
      Type: AWS::EC2::VPCEndpoint
      Properties:
        ServiceName: !Sub com.amazonaws.${AWS::Region}.secretsmanager
        VpcEndpointType: Interface
        VpcId: vpc-04a901e4d43c1729d
        SubnetIds:
          - subnet-048b31cc2b20b55c7
        SecurityGroupIds:
          - sg-0f1753bbc3f41424c
        PrivateDnsEnabled: true